server:
  port: ${port:8081}

data:
  mongodb:
    uri: mongodb://localhost:27017/gallerydb

spring:
  data:
    mongodb:
      port: 27017

  cloud:
    config:
      override-system-properties: false

